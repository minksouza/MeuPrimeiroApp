import {StyleSheet} from "react-native"

export const styles = StyleSheet.create({
    button:{
        width: "100%",
        height: 52,
        backgroundColor: "red",
        borderRadius: 10,
        justifyContent: "center",
        alignItems: "center"
    },

    title:{
        fontSize: 14,
        fontWeight: "bold",
        color: "white"
    }
})